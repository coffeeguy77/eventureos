# Supabase project: eventureos (ap-southeast-2)
NEXT_PUBLIC_SUPABASE_URL=https://hrarethuqwfmtewxngxt.supabase.co
# Publishable key — safe for the browser; Row Level Security protects the data
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_MtG4hQs5ADkozmj1gSc3NQ_gl17nY5E

# --- Server secrets (set in Vercel as "Sensitive"; never exposed to the browser) ---
# Random 32-byte hex strings. INTEGRATION_SERVER_KEY's sha256 must match private.server_keys
# (name = 'integration') in the database — rotate both together.
INTEGRATION_SERVER_KEY=
OAUTH_STATE_SECRET=
CRON_SECRET=

# --- Integrations (fill in after creating the Google Cloud and Xero apps) ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
XERO_CLIENT_ID=
XERO_CLIENT_SECRET=
# Optional: background sync every day (Settings → Integrations "Sync now" works without it)
SUPABASE_SERVICE_ROLE_KEY=
# Optional: AI enquiry classification / drafting
ANTHROPIC_API_KEY=
