export const PIPELINE_ENQUIRY_STAGES = ["new", "contacted", "qualified", "quote_required", "quote_sent", "negotiating", "won"] as const;
export const PIPELINE_EVENT_STAGES = ["planning", "quoted", "awaiting_approval", "confirmed", "completed"] as const;
